{
  "compilerOptions": {
    "target": "ESNext",                // Define a versão do JavaScript para a qual o TypeScript será compilado.
    "module": "commonjs",              // Define o sistema de módulos para CommonJS, usado no Node.js.
    "strict": true,                    // Ativa todas as verificações de tipo estritas.
    "esModuleInterop": true,           // Permite a interoperabilidade entre módulos ES e CommonJS.
    "skipLibCheck": true,              // Ignora a verificação de tipos nos arquivos de declaração.
    "forceConsistentCasingInFileNames": true, // Força o uso consistente de maiúsculas e minúsculas nos nomes de arquivos.
    "outDir": "./dist",                // Diretório de saída para os arquivos compilados.
    "rootDir": "./src",                // Diretório raiz dos arquivos de entrada.
    "resolveJsonModule": true,         // Permite a importação de módulos JSON.
    "moduleResolution": "node",        // Usa a resolução de módulos do Node.js.
    "allowSyntheticDefaultImports": true, // Permite importações padrão sintéticas.
    "experimentalDecorators": true,       // Adicione esta linha
    "emitDecoratorMetadata": true         // Adicione esta linha
  },
  "include": ["src/**/*.ts"],          // Inclui todos os arquivos TypeScript dentro do diretório `src`.
  "exclude": ["node_modules"]          // Exclui o diretório `node_modules` da compilação.
}
